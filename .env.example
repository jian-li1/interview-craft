# Root env used by docker-compose (frontend build args).
# App-specific env lives in backend/.env and frontend/.env.local — see those .env.example files.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
